// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { PreviousPlayerUsernameState } from "./previous_player_username_state_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `previous_player_username_state`.
 *
 * Obtain a handle from the [`previousPlayerUsernameState`] property on [`RemoteTables`],
 * like `ctx.db.previousPlayerUsernameState`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.previousPlayerUsernameState.on_insert(...)`.
 */
export class PreviousPlayerUsernameStateTableHandle {
  tableCache: TableCache<PreviousPlayerUsernameState>;

  constructor(tableCache: TableCache<PreviousPlayerUsernameState>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PreviousPlayerUsernameState> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `identity` unique index on the table `previous_player_username_state`,
   * which allows point queries on the field of the same name
   * via the [`PreviousPlayerUsernameStateIdentityUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.previousPlayerUsernameState.identity().find(...)`.
   *
   * Get a handle on the `identity` unique index on the table `previous_player_username_state`.
   */
  identity = {
    // Find the subscribed row whose `identity` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): PreviousPlayerUsernameState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.identity, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `name` unique index on the table `previous_player_username_state`,
   * which allows point queries on the field of the same name
   * via the [`PreviousPlayerUsernameStateNameUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.previousPlayerUsernameState.name().find(...)`.
   *
   * Get a handle on the `name` unique index on the table `previous_player_username_state`.
   */
  name = {
    // Find the subscribed row whose `name` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): PreviousPlayerUsernameState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.name, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `lowerCaseName` unique index on the table `previous_player_username_state`,
   * which allows point queries on the field of the same name
   * via the [`PreviousPlayerUsernameStateLowerCaseNameUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.previousPlayerUsernameState.lowerCaseName().find(...)`.
   *
   * Get a handle on the `lowerCaseName` unique index on the table `previous_player_username_state`.
   */
  lowerCaseName = {
    // Find the subscribed row whose `lowerCaseName` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): PreviousPlayerUsernameState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.lowerCaseName, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PreviousPlayerUsernameState) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PreviousPlayerUsernameState) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PreviousPlayerUsernameState) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PreviousPlayerUsernameState) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }
}
