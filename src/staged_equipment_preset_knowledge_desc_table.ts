// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit ).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { EquipmentPresetKnowledgeDesc } from "./equipment_preset_knowledge_desc_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `staged_equipment_preset_knowledge_desc`.
 *
 * Obtain a handle from the [`stagedEquipmentPresetKnowledgeDesc`] property on [`RemoteTables`],
 * like `ctx.db.stagedEquipmentPresetKnowledgeDesc`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.stagedEquipmentPresetKnowledgeDesc.on_insert(...)`.
 */
export class StagedEquipmentPresetKnowledgeDescTableHandle {
  tableCache: TableCache<EquipmentPresetKnowledgeDesc>;

  constructor(tableCache: TableCache<EquipmentPresetKnowledgeDesc>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<EquipmentPresetKnowledgeDesc> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `knowledgeId` unique index on the table `staged_equipment_preset_knowledge_desc`,
   * which allows point queries on the field of the same name
   * via the [`StagedEquipmentPresetKnowledgeDescKnowledgeIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.stagedEquipmentPresetKnowledgeDesc.knowledgeId().find(...)`.
   *
   * Get a handle on the `knowledgeId` unique index on the table `staged_equipment_preset_knowledge_desc`.
   */
  knowledgeId = {
    // Find the subscribed row whose `knowledgeId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): EquipmentPresetKnowledgeDesc | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.knowledgeId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: EquipmentPresetKnowledgeDesc) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: EquipmentPresetKnowledgeDesc) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: EquipmentPresetKnowledgeDesc) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: EquipmentPresetKnowledgeDesc) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: EquipmentPresetKnowledgeDesc, newRow: EquipmentPresetKnowledgeDesc) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: EquipmentPresetKnowledgeDesc, newRow: EquipmentPresetKnowledgeDesc) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
