// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit ).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { ModReplacementTextState } from "./mod_replacement_text_state_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `mod_replacement_text_state`.
 *
 * Obtain a handle from the [`modReplacementTextState`] property on [`RemoteTables`],
 * like `ctx.db.modReplacementTextState`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.modReplacementTextState.on_insert(...)`.
 */
export class ModReplacementTextStateTableHandle {
  tableCache: TableCache<ModReplacementTextState>;

  constructor(tableCache: TableCache<ModReplacementTextState>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<ModReplacementTextState> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `textType` unique index on the table `mod_replacement_text_state`,
   * which allows point queries on the field of the same name
   * via the [`ModReplacementTextStateTextTypeUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.modReplacementTextState.textType().find(...)`.
   *
   * Get a handle on the `textType` unique index on the table `mod_replacement_text_state`.
   */
  textType = {
    // Find the subscribed row whose `textType` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): ModReplacementTextState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.textType, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: ModReplacementTextState) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: ModReplacementTextState) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: ModReplacementTextState) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: ModReplacementTextState) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: ModReplacementTextState, newRow: ModReplacementTextState) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: ModReplacementTextState, newRow: ModReplacementTextState) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
