// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { ClaimTileCost } from "./claim_tile_cost_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `staged_claim_tile_cost`.
 *
 * Obtain a handle from the [`stagedClaimTileCost`] property on [`RemoteTables`],
 * like `ctx.db.stagedClaimTileCost`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.stagedClaimTileCost.on_insert(...)`.
 */
export class StagedClaimTileCostTableHandle {
  tableCache: TableCache<ClaimTileCost>;

  constructor(tableCache: TableCache<ClaimTileCost>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<ClaimTileCost> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `tileCount` unique index on the table `staged_claim_tile_cost`,
   * which allows point queries on the field of the same name
   * via the [`StagedClaimTileCostTileCountUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.stagedClaimTileCost.tileCount().find(...)`.
   *
   * Get a handle on the `tileCount` unique index on the table `staged_claim_tile_cost`.
   */
  tileCount = {
    // Find the subscribed row whose `tileCount` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): ClaimTileCost | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.tileCount, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: ClaimTileCost) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: ClaimTileCost) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: ClaimTileCost) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: ClaimTileCost) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: ClaimTileCost, newRow: ClaimTileCost) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: ClaimTileCost, newRow: ClaimTileCost) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
