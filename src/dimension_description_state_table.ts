// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
import {
  AlgebraicType as __AlgebraicTypeValue,
  BinaryReader as __BinaryReader,
  BinaryWriter as __BinaryWriter,
  ClientCache as __ClientCache,
  ConnectionId as __ConnectionId,
  DbConnectionBuilder as __DbConnectionBuilder,
  DbConnectionImpl as __DbConnectionImpl,
  Identity as __Identity,
  SubscriptionBuilderImpl as __SubscriptionBuilderImpl,
  TableCache as __TableCache,
  TimeDuration as __TimeDuration,
  Timestamp as __Timestamp,
  deepEqual as __deepEqual,
  type AlgebraicType as __AlgebraicTypeType,
  type AlgebraicTypeVariants as __AlgebraicTypeVariants,
  type CallReducerFlags as __CallReducerFlags,
  type ErrorContextInterface as __ErrorContextInterface,
  type Event as __Event,
  type EventContextInterface as __EventContextInterface,
  type ReducerEventContextInterface as __ReducerEventContextInterface,
  type SubscriptionEventContextInterface as __SubscriptionEventContextInterface,
} from "spacetimedb";
import { DimensionDescriptionState } from "./dimension_description_state_type";
import { DimensionType } from "./dimension_type_type";
// Mark import as potentially unused
declare type __keep_DimensionType = DimensionType;

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";
declare type __keep = [EventContext, Reducer, RemoteReducers, RemoteTables];

/**
 * Table handle for the table `dimension_description_state`.
 *
 * Obtain a handle from the [`dimensionDescriptionState`] property on [`RemoteTables`],
 * like `ctx.db.dimensionDescriptionState`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.dimensionDescriptionState.on_insert(...)`.
 */
export class DimensionDescriptionStateTableHandle {
  tableCache: __TableCache<DimensionDescriptionState>;

  constructor(tableCache: __TableCache<DimensionDescriptionState>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<DimensionDescriptionState> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `entityId` unique index on the table `dimension_description_state`,
   * which allows point queries on the field of the same name
   * via the [`DimensionDescriptionStateEntityIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.dimensionDescriptionState.entityId().find(...)`.
   *
   * Get a handle on the `entityId` unique index on the table `dimension_description_state`.
   */
  entityId = {
    // Find the subscribed row whose `entityId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): DimensionDescriptionState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.entityId, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `dimensionId` unique index on the table `dimension_description_state`,
   * which allows point queries on the field of the same name
   * via the [`DimensionDescriptionStateDimensionIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.dimensionDescriptionState.dimensionId().find(...)`.
   *
   * Get a handle on the `dimensionId` unique index on the table `dimension_description_state`.
   */
  dimensionId = {
    // Find the subscribed row whose `dimensionId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): DimensionDescriptionState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.dimensionId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: DimensionDescriptionState) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: DimensionDescriptionState) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: DimensionDescriptionState) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: DimensionDescriptionState) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: DimensionDescriptionState, newRow: DimensionDescriptionState) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: DimensionDescriptionState, newRow: DimensionDescriptionState) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
