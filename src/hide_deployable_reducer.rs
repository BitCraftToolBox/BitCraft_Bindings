// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::hide_deployable_timer_type::HideDeployableTimer;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct HideDeployableArgs {
    pub timer: HideDeployableTimer,
}

impl From<HideDeployableArgs> for super::Reducer {
    fn from(args: HideDeployableArgs) -> Self {
        Self::HideDeployable { timer: args.timer }
    }
}

impl __sdk::InModule for HideDeployableArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `hide_deployable`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait hide_deployable {
    /// Request that the remote module invoke the reducer `hide_deployable` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`hide_deployable:hide_deployable_then`] to run a callback after the reducer completes.
    fn hide_deployable(&self, timer: HideDeployableTimer) -> __sdk::Result<()> {
        self.hide_deployable_then(timer, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `hide_deployable` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn hide_deployable_then(
        &self,
        timer: HideDeployableTimer,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl hide_deployable for super::RemoteReducers {
    fn hide_deployable_then(
        &self,
        timer: HideDeployableTimer,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(HideDeployableArgs { timer }, callback)
    }
}
