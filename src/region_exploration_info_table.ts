// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit ).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { RegionExplorationInfo } from "./region_exploration_info_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `region_exploration_info`.
 *
 * Obtain a handle from the [`regionExplorationInfo`] property on [`RemoteTables`],
 * like `ctx.db.regionExplorationInfo`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.regionExplorationInfo.on_insert(...)`.
 */
export class RegionExplorationInfoTableHandle {
  tableCache: TableCache<RegionExplorationInfo>;

  constructor(tableCache: TableCache<RegionExplorationInfo>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<RegionExplorationInfo> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `regionId` unique index on the table `region_exploration_info`,
   * which allows point queries on the field of the same name
   * via the [`RegionExplorationInfoRegionIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.regionExplorationInfo.regionId().find(...)`.
   *
   * Get a handle on the `regionId` unique index on the table `region_exploration_info`.
   */
  regionId = {
    // Find the subscribed row whose `regionId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): RegionExplorationInfo | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.regionId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: RegionExplorationInfo) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: RegionExplorationInfo) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: RegionExplorationInfo) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: RegionExplorationInfo) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: RegionExplorationInfo, newRow: RegionExplorationInfo) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: RegionExplorationInfo, newRow: RegionExplorationInfo) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
