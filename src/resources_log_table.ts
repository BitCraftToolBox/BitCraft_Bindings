// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
import {
  AlgebraicType as __AlgebraicTypeValue,
  BinaryReader as __BinaryReader,
  BinaryWriter as __BinaryWriter,
  ClientCache as __ClientCache,
  ConnectionId as __ConnectionId,
  DbConnectionBuilder as __DbConnectionBuilder,
  DbConnectionImpl as __DbConnectionImpl,
  Identity as __Identity,
  SubscriptionBuilderImpl as __SubscriptionBuilderImpl,
  TableCache as __TableCache,
  TimeDuration as __TimeDuration,
  Timestamp as __Timestamp,
  deepEqual as __deepEqual,
  type AlgebraicType as __AlgebraicTypeType,
  type AlgebraicTypeVariants as __AlgebraicTypeVariants,
  type CallReducerFlags as __CallReducerFlags,
  type ErrorContextInterface as __ErrorContextInterface,
  type Event as __Event,
  type EventContextInterface as __EventContextInterface,
  type ReducerEventContextInterface as __ReducerEventContextInterface,
  type SubscriptionEventContextInterface as __SubscriptionEventContextInterface,
} from "spacetimedb";
import { ResourcesLog } from "./resources_log_type";
import { ResourceClumpInfo } from "./resource_clump_info_type";
// Mark import as potentially unused
declare type __keep_ResourceClumpInfo = ResourceClumpInfo;
import { ResourceInfo } from "./resource_info_type";
// Mark import as potentially unused
declare type __keep_ResourceInfo = ResourceInfo;
import { CommonRng } from "./common_rng_type";
// Mark import as potentially unused
declare type __keep_CommonRng = CommonRng;

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";
declare type __keep = [EventContext, Reducer, RemoteReducers, RemoteTables];

/**
 * Table handle for the table `resources_log`.
 *
 * Obtain a handle from the [`resourcesLog`] property on [`RemoteTables`],
 * like `ctx.db.resourcesLog`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.resourcesLog.on_insert(...)`.
 */
export class ResourcesLogTableHandle {
  tableCache: __TableCache<ResourcesLog>;

  constructor(tableCache: __TableCache<ResourcesLog>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<ResourcesLog> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `version` unique index on the table `resources_log`,
   * which allows point queries on the field of the same name
   * via the [`ResourcesLogVersionUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.resourcesLog.version().find(...)`.
   *
   * Get a handle on the `version` unique index on the table `resources_log`.
   */
  version = {
    // Find the subscribed row whose `version` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): ResourcesLog | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.version, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: ResourcesLog) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: ResourcesLog) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: ResourcesLog) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: ResourcesLog) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: ResourcesLog, newRow: ResourcesLog) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: ResourcesLog, newRow: ResourcesLog) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
