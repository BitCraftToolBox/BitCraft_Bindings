// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit ).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { EmpireLowercaseNameState } from "./empire_lowercase_name_state_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `empire_lowercase_name_state`.
 *
 * Obtain a handle from the [`empireLowercaseNameState`] property on [`RemoteTables`],
 * like `ctx.db.empireLowercaseNameState`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.empireLowercaseNameState.on_insert(...)`.
 */
export class EmpireLowercaseNameStateTableHandle {
  tableCache: TableCache<EmpireLowercaseNameState>;

  constructor(tableCache: TableCache<EmpireLowercaseNameState>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<EmpireLowercaseNameState> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `entityId` unique index on the table `empire_lowercase_name_state`,
   * which allows point queries on the field of the same name
   * via the [`EmpireLowercaseNameStateEntityIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.empireLowercaseNameState.entityId().find(...)`.
   *
   * Get a handle on the `entityId` unique index on the table `empire_lowercase_name_state`.
   */
  entityId = {
    // Find the subscribed row whose `entityId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): EmpireLowercaseNameState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.entityId, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `nameLowercase` unique index on the table `empire_lowercase_name_state`,
   * which allows point queries on the field of the same name
   * via the [`EmpireLowercaseNameStateNameLowercaseUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.empireLowercaseNameState.nameLowercase().find(...)`.
   *
   * Get a handle on the `nameLowercase` unique index on the table `empire_lowercase_name_state`.
   */
  nameLowercase = {
    // Find the subscribed row whose `nameLowercase` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): EmpireLowercaseNameState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.nameLowercase, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: EmpireLowercaseNameState) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: EmpireLowercaseNameState) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: EmpireLowercaseNameState) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: EmpireLowercaseNameState) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: EmpireLowercaseNameState, newRow: EmpireLowercaseNameState) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: EmpireLowercaseNameState, newRow: EmpireLowercaseNameState) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
