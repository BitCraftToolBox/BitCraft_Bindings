// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { BuildingPortalDescV2 } from "./building_portal_desc_v_2_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `building_portal_desc_v2`.
 *
 * Obtain a handle from the [`buildingPortalDescV2`] property on [`RemoteTables`],
 * like `ctx.db.buildingPortalDescV2`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.buildingPortalDescV2.on_insert(...)`.
 */
export class BuildingPortalDescV2TableHandle {
  tableCache: TableCache<BuildingPortalDescV2>;

  constructor(tableCache: TableCache<BuildingPortalDescV2>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<BuildingPortalDescV2> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `id` unique index on the table `building_portal_desc_v2`,
   * which allows point queries on the field of the same name
   * via the [`BuildingPortalDescV2IdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.buildingPortalDescV2.id().find(...)`.
   *
   * Get a handle on the `id` unique index on the table `building_portal_desc_v2`.
   */
  id = {
    // Find the subscribed row whose `id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): BuildingPortalDescV2 | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: BuildingPortalDescV2) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: BuildingPortalDescV2) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: BuildingPortalDescV2) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: BuildingPortalDescV2) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: BuildingPortalDescV2, newRow: BuildingPortalDescV2) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: BuildingPortalDescV2, newRow: BuildingPortalDescV2) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
