// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit ).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { PlaceableGrowthDesc } from "./placeable_growth_desc_type";
import { PlaceableGrowthOutcome as __PlaceableGrowthOutcome } from "./placeable_growth_outcome_type";

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `staged_placeable_growth_desc`.
 *
 * Obtain a handle from the [`stagedPlaceableGrowthDesc`] property on [`RemoteTables`],
 * like `ctx.db.stagedPlaceableGrowthDesc`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.stagedPlaceableGrowthDesc.on_insert(...)`.
 */
export class StagedPlaceableGrowthDescTableHandle {
  tableCache: TableCache<PlaceableGrowthDesc>;

  constructor(tableCache: TableCache<PlaceableGrowthDesc>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PlaceableGrowthDesc> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `id` unique index on the table `staged_placeable_growth_desc`,
   * which allows point queries on the field of the same name
   * via the [`StagedPlaceableGrowthDescIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.stagedPlaceableGrowthDesc.id().find(...)`.
   *
   * Get a handle on the `id` unique index on the table `staged_placeable_growth_desc`.
   */
  id = {
    // Find the subscribed row whose `id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): PlaceableGrowthDesc | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.id, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `placeableId` unique index on the table `staged_placeable_growth_desc`,
   * which allows point queries on the field of the same name
   * via the [`StagedPlaceableGrowthDescPlaceableIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.stagedPlaceableGrowthDesc.placeableId().find(...)`.
   *
   * Get a handle on the `placeableId` unique index on the table `staged_placeable_growth_desc`.
   */
  placeableId = {
    // Find the subscribed row whose `placeableId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): PlaceableGrowthDesc | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.placeableId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PlaceableGrowthDesc) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PlaceableGrowthDesc) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PlaceableGrowthDesc) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PlaceableGrowthDesc) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PlaceableGrowthDesc, newRow: PlaceableGrowthDesc) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PlaceableGrowthDesc, newRow: PlaceableGrowthDesc) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
