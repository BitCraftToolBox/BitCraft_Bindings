// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::loot_chest_despawn_timer_type::LootChestDespawnTimer;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LootChestDespawnArgs {
    pub timer: LootChestDespawnTimer,
}

impl From<LootChestDespawnArgs> for super::Reducer {
    fn from(args: LootChestDespawnArgs) -> Self {
        Self::LootChestDespawn { timer: args.timer }
    }
}

impl __sdk::InModule for LootChestDespawnArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `loot_chest_despawn`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait loot_chest_despawn {
    /// Request that the remote module invoke the reducer `loot_chest_despawn` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`loot_chest_despawn:loot_chest_despawn_then`] to run a callback after the reducer completes.
    fn loot_chest_despawn(&self, timer: LootChestDespawnTimer) -> __sdk::Result<()> {
        self.loot_chest_despawn_then(timer, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `loot_chest_despawn` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn loot_chest_despawn_then(
        &self,
        timer: LootChestDespawnTimer,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl loot_chest_despawn for super::RemoteReducers {
    fn loot_chest_despawn_then(
        &self,
        timer: LootChestDespawnTimer,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(LootChestDespawnArgs { timer }, callback)
    }
}
