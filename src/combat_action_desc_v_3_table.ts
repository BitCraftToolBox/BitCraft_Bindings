// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { CombatActionDescV3 } from "./combat_action_desc_v_3_type";
import { BuffEffect as __BuffEffect } from "./buff_effect_type";
import { LevelRequirement as __LevelRequirement } from "./level_requirement_type";

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `combat_action_desc_v3`.
 *
 * Obtain a handle from the [`combatActionDescV3`] property on [`RemoteTables`],
 * like `ctx.db.combatActionDescV3`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.combatActionDescV3.on_insert(...)`.
 */
export class CombatActionDescV3TableHandle {
  tableCache: TableCache<CombatActionDescV3>;

  constructor(tableCache: TableCache<CombatActionDescV3>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<CombatActionDescV3> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `id` unique index on the table `combat_action_desc_v3`,
   * which allows point queries on the field of the same name
   * via the [`CombatActionDescV3IdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.combatActionDescV3.id().find(...)`.
   *
   * Get a handle on the `id` unique index on the table `combat_action_desc_v3`.
   */
  id = {
    // Find the subscribed row whose `id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): CombatActionDescV3 | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: CombatActionDescV3) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: CombatActionDescV3) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: CombatActionDescV3) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: CombatActionDescV3) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: CombatActionDescV3, newRow: CombatActionDescV3) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: CombatActionDescV3, newRow: CombatActionDescV3) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
