// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { PreviousEmpireNameState } from "./previous_empire_name_state_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `previous_empire_name_state`.
 *
 * Obtain a handle from the [`previousEmpireNameState`] property on [`RemoteTables`],
 * like `ctx.db.previousEmpireNameState`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.previousEmpireNameState.on_insert(...)`.
 */
export class PreviousEmpireNameStateTableHandle {
  tableCache: TableCache<PreviousEmpireNameState>;

  constructor(tableCache: TableCache<PreviousEmpireNameState>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PreviousEmpireNameState> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `emperorIdentity` unique index on the table `previous_empire_name_state`,
   * which allows point queries on the field of the same name
   * via the [`PreviousEmpireNameStateEmperorIdentityUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.previousEmpireNameState.emperorIdentity().find(...)`.
   *
   * Get a handle on the `emperorIdentity` unique index on the table `previous_empire_name_state`.
   */
  emperorIdentity = {
    // Find the subscribed row whose `emperorIdentity` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): PreviousEmpireNameState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.emperorIdentity, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `empireName` unique index on the table `previous_empire_name_state`,
   * which allows point queries on the field of the same name
   * via the [`PreviousEmpireNameStateEmpireNameUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.previousEmpireNameState.empireName().find(...)`.
   *
   * Get a handle on the `empireName` unique index on the table `previous_empire_name_state`.
   */
  empireName = {
    // Find the subscribed row whose `empireName` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): PreviousEmpireNameState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.empireName, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `empireLowerCaseName` unique index on the table `previous_empire_name_state`,
   * which allows point queries on the field of the same name
   * via the [`PreviousEmpireNameStateEmpireLowerCaseNameUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.previousEmpireNameState.empireLowerCaseName().find(...)`.
   *
   * Get a handle on the `empireLowerCaseName` unique index on the table `previous_empire_name_state`.
   */
  empireLowerCaseName = {
    // Find the subscribed row whose `empireLowerCaseName` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): PreviousEmpireNameState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.empireLowerCaseName, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PreviousEmpireNameState) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PreviousEmpireNameState) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PreviousEmpireNameState) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PreviousEmpireNameState) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PreviousEmpireNameState, newRow: PreviousEmpireNameState) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PreviousEmpireNameState, newRow: PreviousEmpireNameState) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
