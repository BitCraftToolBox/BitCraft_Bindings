// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { DeployableDescV2 } from "./deployable_desc_v_2_type";
import { CsvStatEntry as __CsvStatEntry } from "./csv_stat_entry_type";
import { ExperienceStackF32 as __ExperienceStackF32 } from "./experience_stack_f_32_type";
import { DeployableType as __DeployableType } from "./deployable_type_type";
import { MovementType as __MovementType } from "./movement_type_type";
import { MovementSpeed as __MovementSpeed } from "./movement_speed_type";

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `deployable_desc_v2`.
 *
 * Obtain a handle from the [`deployableDescV2`] property on [`RemoteTables`],
 * like `ctx.db.deployableDescV2`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.deployableDescV2.on_insert(...)`.
 */
export class DeployableDescV2TableHandle {
  tableCache: TableCache<DeployableDescV2>;

  constructor(tableCache: TableCache<DeployableDescV2>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<DeployableDescV2> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `id` unique index on the table `deployable_desc_v2`,
   * which allows point queries on the field of the same name
   * via the [`DeployableDescV2IdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.deployableDescV2.id().find(...)`.
   *
   * Get a handle on the `id` unique index on the table `deployable_desc_v2`.
   */
  id = {
    // Find the subscribed row whose `id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): DeployableDescV2 | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.id, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `deployFromCollectibleId` unique index on the table `deployable_desc_v2`,
   * which allows point queries on the field of the same name
   * via the [`DeployableDescV2DeployFromCollectibleIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.deployableDescV2.deployFromCollectibleId().find(...)`.
   *
   * Get a handle on the `deployFromCollectibleId` unique index on the table `deployable_desc_v2`.
   */
  deployFromCollectibleId = {
    // Find the subscribed row whose `deployFromCollectibleId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): DeployableDescV2 | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.deployFromCollectibleId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: DeployableDescV2) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: DeployableDescV2) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: DeployableDescV2) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: DeployableDescV2) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: DeployableDescV2, newRow: DeployableDescV2) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: DeployableDescV2, newRow: DeployableDescV2) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
