// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit ).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { PreviousPlayerSkillsState } from "./previous_player_skills_state_type";
import { ExperienceStack as __ExperienceStack } from "./experience_stack_type";

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `previous_player_skills_state`.
 *
 * Obtain a handle from the [`previousPlayerSkillsState`] property on [`RemoteTables`],
 * like `ctx.db.previousPlayerSkillsState`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.previousPlayerSkillsState.on_insert(...)`.
 */
export class PreviousPlayerSkillsStateTableHandle {
  tableCache: TableCache<PreviousPlayerSkillsState>;

  constructor(tableCache: TableCache<PreviousPlayerSkillsState>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PreviousPlayerSkillsState> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `identity` unique index on the table `previous_player_skills_state`,
   * which allows point queries on the field of the same name
   * via the [`PreviousPlayerSkillsStateIdentityUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.previousPlayerSkillsState.identity().find(...)`.
   *
   * Get a handle on the `identity` unique index on the table `previous_player_skills_state`.
   */
  identity = {
    // Find the subscribed row whose `identity` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): PreviousPlayerSkillsState | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.identity, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PreviousPlayerSkillsState) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PreviousPlayerSkillsState) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PreviousPlayerSkillsState) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PreviousPlayerSkillsState) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PreviousPlayerSkillsState, newRow: PreviousPlayerSkillsState) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PreviousPlayerSkillsState, newRow: PreviousPlayerSkillsState) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
