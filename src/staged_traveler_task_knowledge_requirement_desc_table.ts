// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { TravelerTaskKnowledgeRequirementDesc } from "./traveler_task_knowledge_requirement_desc_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `staged_traveler_task_knowledge_requirement_desc`.
 *
 * Obtain a handle from the [`stagedTravelerTaskKnowledgeRequirementDesc`] property on [`RemoteTables`],
 * like `ctx.db.stagedTravelerTaskKnowledgeRequirementDesc`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.stagedTravelerTaskKnowledgeRequirementDesc.on_insert(...)`.
 */
export class StagedTravelerTaskKnowledgeRequirementDescTableHandle {
  tableCache: TableCache<TravelerTaskKnowledgeRequirementDesc>;

  constructor(tableCache: TableCache<TravelerTaskKnowledgeRequirementDesc>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<TravelerTaskKnowledgeRequirementDesc> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `travelerTaskId` unique index on the table `staged_traveler_task_knowledge_requirement_desc`,
   * which allows point queries on the field of the same name
   * via the [`StagedTravelerTaskKnowledgeRequirementDescTravelerTaskIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.stagedTravelerTaskKnowledgeRequirementDesc.travelerTaskId().find(...)`.
   *
   * Get a handle on the `travelerTaskId` unique index on the table `staged_traveler_task_knowledge_requirement_desc`.
   */
  travelerTaskId = {
    // Find the subscribed row whose `travelerTaskId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): TravelerTaskKnowledgeRequirementDesc | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.travelerTaskId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: TravelerTaskKnowledgeRequirementDesc) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: TravelerTaskKnowledgeRequirementDesc) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: TravelerTaskKnowledgeRequirementDesc) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: TravelerTaskKnowledgeRequirementDesc) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: TravelerTaskKnowledgeRequirementDesc, newRow: TravelerTaskKnowledgeRequirementDesc) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: TravelerTaskKnowledgeRequirementDesc, newRow: TravelerTaskKnowledgeRequirementDesc) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
