// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace BitCraftRegion.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void PlayerRerollTravelerTaskHandler(ReducerEventContext ctx, ulong taskEntityId);
        public event PlayerRerollTravelerTaskHandler? OnPlayerRerollTravelerTask;

        public void PlayerRerollTravelerTask(ulong taskEntityId)
        {
            conn.InternalCallReducer(new Reducer.PlayerRerollTravelerTask(taskEntityId), this.SetCallReducerFlags.PlayerRerollTravelerTaskFlags);
        }

        public bool InvokePlayerRerollTravelerTask(ReducerEventContext ctx, Reducer.PlayerRerollTravelerTask args)
        {
            if (OnPlayerRerollTravelerTask == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnPlayerRerollTravelerTask(
                ctx,
                args.TaskEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class PlayerRerollTravelerTask : Reducer, IReducerArgs
        {
            [DataMember(Name = "task_entity_id")]
            public ulong TaskEntityId;

            public PlayerRerollTravelerTask(ulong TaskEntityId)
            {
                this.TaskEntityId = TaskEntityId;
            }

            public PlayerRerollTravelerTask()
            {
            }

            string IReducerArgs.ReducerName => "player_reroll_traveler_task";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags PlayerRerollTravelerTaskFlags;
        public void PlayerRerollTravelerTask(CallReducerFlags flags) => PlayerRerollTravelerTaskFlags = flags;
    }
}
