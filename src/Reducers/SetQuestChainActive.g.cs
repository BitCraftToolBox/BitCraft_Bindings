// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


#nullable enable

using System;
using SpacetimeDB;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace BitCraftRegion.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void SetQuestChainActiveHandler(ReducerEventContext ctx, int id);
        public event SetQuestChainActiveHandler? OnSetQuestChainActive;

        public void SetQuestChainActive(int id)
        {
            conn.InternalCallReducer(new Reducer.SetQuestChainActive(id), this.SetCallReducerFlags.SetQuestChainActiveFlags);
        }

        public bool InvokeSetQuestChainActive(ReducerEventContext ctx, Reducer.SetQuestChainActive args)
        {
            if (OnSetQuestChainActive == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnSetQuestChainActive(
                ctx,
                args.Id
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class SetQuestChainActive : Reducer, IReducerArgs
        {
            [DataMember(Name = "id")]
            public int Id;

            public SetQuestChainActive(int Id)
            {
                this.Id = Id;
            }

            public SetQuestChainActive()
            {
            }

            string IReducerArgs.ReducerName => "set_quest_chain_active";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags SetQuestChainActiveFlags;
        public void SetQuestChainActive(CallReducerFlags flags) => SetQuestChainActiveFlags = flags;
    }
}
