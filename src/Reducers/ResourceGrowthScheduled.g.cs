// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace BitCraftRegion.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ResourceGrowthScheduledHandler(ReducerEventContext ctx, BitCraftRegion.Types.GrowthTimer timer);
        public event ResourceGrowthScheduledHandler? OnResourceGrowthScheduled;

        public void ResourceGrowthScheduled(BitCraftRegion.Types.GrowthTimer timer)
        {
            conn.InternalCallReducer(new Reducer.ResourceGrowthScheduled(timer), this.SetCallReducerFlags.ResourceGrowthScheduledFlags);
        }

        public bool InvokeResourceGrowthScheduled(ReducerEventContext ctx, Reducer.ResourceGrowthScheduled args)
        {
            if (OnResourceGrowthScheduled == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnResourceGrowthScheduled(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ResourceGrowthScheduled : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public GrowthTimer Timer;

            public ResourceGrowthScheduled(GrowthTimer Timer)
            {
                this.Timer = Timer;
            }

            public ResourceGrowthScheduled()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "resource_growth_scheduled";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ResourceGrowthScheduledFlags;
        public void ResourceGrowthScheduled(CallReducerFlags flags) => ResourceGrowthScheduledFlags = flags;
    }
}
