// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.


#nullable enable

using System;
using SpacetimeDB;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace BitCraftGlobal.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AdminSetShardsHandler(ReducerEventContext ctx, string identity, int amount);
        public event AdminSetShardsHandler? OnAdminSetShards;

        public void AdminSetShards(string identity, int amount)
        {
            conn.InternalCallReducer(new Reducer.AdminSetShards(identity, amount), this.SetCallReducerFlags.AdminSetShardsFlags);
        }

        public bool InvokeAdminSetShards(ReducerEventContext ctx, Reducer.AdminSetShards args)
        {
            if (OnAdminSetShards == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAdminSetShards(
                ctx,
                args.Identity,
                args.Amount
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class AdminSetShards : Reducer, IReducerArgs
        {
            [DataMember(Name = "identity")]
            public string Identity;
            [DataMember(Name = "amount")]
            public int Amount;

            public AdminSetShards(
                string Identity,
                int Amount
            )
            {
                this.Identity = Identity;
                this.Amount = Amount;
            }

            public AdminSetShards()
            {
                this.Identity = "";
            }

            string IReducerArgs.ReducerName => "admin_set_shards";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags AdminSetShardsFlags;
        public void AdminSetShards(CallReducerFlags flags) => AdminSetShardsFlags = flags;
    }
}
