// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
import {
  AlgebraicType as __AlgebraicTypeValue,
  BinaryReader as __BinaryReader,
  BinaryWriter as __BinaryWriter,
  ClientCache as __ClientCache,
  ConnectionId as __ConnectionId,
  DbConnectionBuilder as __DbConnectionBuilder,
  DbConnectionImpl as __DbConnectionImpl,
  Identity as __Identity,
  SubscriptionBuilderImpl as __SubscriptionBuilderImpl,
  TableCache as __TableCache,
  TimeDuration as __TimeDuration,
  Timestamp as __Timestamp,
  deepEqual as __deepEqual,
  type AlgebraicType as __AlgebraicTypeType,
  type AlgebraicTypeVariants as __AlgebraicTypeVariants,
  type CallReducerFlags as __CallReducerFlags,
  type ErrorContextInterface as __ErrorContextInterface,
  type Event as __Event,
  type EventContextInterface as __EventContextInterface,
  type ReducerEventContextInterface as __ReducerEventContextInterface,
  type SubscriptionEventContextInterface as __SubscriptionEventContextInterface,
} from "spacetimedb";
import { EndGracePeriodTimer } from "./end_grace_period_timer_type";
import { GracePeriodType } from "./grace_period_type_type";
// Mark import as potentially unused
declare type __keep_GracePeriodType = GracePeriodType;

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";
declare type __keep = [EventContext, Reducer, RemoteReducers, RemoteTables];

/**
 * Table handle for the table `end_grace_period_timer`.
 *
 * Obtain a handle from the [`endGracePeriodTimer`] property on [`RemoteTables`],
 * like `ctx.db.endGracePeriodTimer`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.endGracePeriodTimer.on_insert(...)`.
 */
export class EndGracePeriodTimerTableHandle {
  tableCache: __TableCache<EndGracePeriodTimer>;

  constructor(tableCache: __TableCache<EndGracePeriodTimer>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<EndGracePeriodTimer> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `scheduledId` unique index on the table `end_grace_period_timer`,
   * which allows point queries on the field of the same name
   * via the [`EndGracePeriodTimerScheduledIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.endGracePeriodTimer.scheduledId().find(...)`.
   *
   * Get a handle on the `scheduledId` unique index on the table `end_grace_period_timer`.
   */
  scheduledId = {
    // Find the subscribed row whose `scheduledId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): EndGracePeriodTimer | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.scheduledId, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `identity` unique index on the table `end_grace_period_timer`,
   * which allows point queries on the field of the same name
   * via the [`EndGracePeriodTimerIdentityUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.endGracePeriodTimer.identity().find(...)`.
   *
   * Get a handle on the `identity` unique index on the table `end_grace_period_timer`.
   */
  identity = {
    // Find the subscribed row whose `identity` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: __Identity): EndGracePeriodTimer | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.identity, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: EndGracePeriodTimer) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: EndGracePeriodTimer) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: EndGracePeriodTimer) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: EndGracePeriodTimer) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: EndGracePeriodTimer, newRow: EndGracePeriodTimer) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: EndGracePeriodTimer, newRow: EndGracePeriodTimer) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
